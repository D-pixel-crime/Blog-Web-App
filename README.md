Download and install node on your desktop
->Download the files
->Go to the file directory
->In command line, run "npm i" to install dependencies
->In command line, run "node index.js"
->Open a web browser and search "localhost:1014"
